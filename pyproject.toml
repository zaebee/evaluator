[project]
name = "llm-benchmark-evaluator"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "pydantic>=2.0",
]

[project.optional-dependencies]
judge = [
    "anthropic>=0.40.0",
    "openai>=1.0.0",
]

[tool.uv]
package = false

[dependency-groups]
dev = ["pytest>=8.0"]
